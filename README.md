# Openship Pilot Demo

Isolated demo application for the Hermes Agent & Openship deployment pilot on `10.100.235.21`.

## Endpoints
- `GET /`: returns message and version
- `GET /health`: returns health status, version, and uptime
